'use client'
import {useEffect,useState} from 'react'
type Campaign={id:string;name:string;source?:string|null;medium?:string|null;campaign?:string|null;links?:unknown[]}
export default function CampaignsPage(){const[campaigns,setCampaigns]=useState<Campaign[]>([]);const[name,setName]=useState('');const[source,setSource]=useState('');const[medium,setMedium]=useState('');const[campaign,setCampaign]=useState('');const[msg,setMsg]=useState('');async function load(){const r=await fetch('/api/campaigns');if(r.ok){const d=await r.json();setCampaigns(d.campaigns||d.data||[])}}useEffect(()=>{void load()},[]);async function create(){setMsg('');const r=await fetch('/api/campaigns',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,source,medium,campaign})});const d=await r.json();if(!r.ok)setMsg(d.error||'Could not create campaign');else{setName('');setMsg('Campaign created');await load()}}return <main className="dashmain"><div className="dashhead"><div><span className="eyebrow">LAXLINK / MARKETING</span><h1>Campaigns</h1><p className="muted">Build reusable UTM campaigns and connect them to links.</p></div></div><section className="card createbox"><div className="creategrid"><label>Name<input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="Summer 2026"/></label><label>Source<input className="input" value={source} onChange={e=>setSource(e.target.value)} placeholder="facebook"/></label><label>Medium<input className="input" value={medium} onChange={e=>setMedium(e.target.value)} placeholder="social"/></label><label>Campaign key<input className="input" value={campaign} onChange={e=>setCampaign(e.target.value)} placeholder="summer-sale"/></label><button className="btn primary createbtn" disabled={!name} onClick={create}>Create campaign</button></div>{msg&&<p className="muted">{msg}</p>}</section><section className="card tablewrap"><table className="table"><thead><tr><th>Name</th><th>Source</th><th>Medium</th><th>Campaign</th><th>Links</th></tr></thead><tbody>{campaigns.map(c=><tr key={c.id}><td>{c.name}</td><td>{c.source||'—'}</td><td>{c.medium||'—'}</td><td>{c.campaign||'—'}</td><td>{c.links?.length||0}</td></tr>)}{!campaigns.length&&<tr><td colSpan={5} className="empty muted">No campaigns yet.</td></tr>}</tbody></table></section></main>}
