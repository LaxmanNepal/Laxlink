'use client'
import {FormEvent,useState} from 'react';import Link from 'next/link'
export default function Register(){const[e,setE]=useState('');const[b,setB]=useState(false);async function s(x:FormEvent<HTMLFormElement>){x.preventDefault();setB(true);setE('');const f=new FormData(x.currentTarget);const r=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),password:f.get('password')})});if(r.ok)location.href='/dashboard';else setE((await r.json()).error||'Registration failed');setB(false)}return <main className="auth-page"><section className="auth-card"><Link href="/" className="brand">LaxLink</Link><h1>Create your workspace</h1><p>Start building smart links and analytics.</p><form onSubmit={s}><label>Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label>{e&&<div className="error">{e}</div>}<button disabled={b}>{b?'Creating…':'Create account'}</button></form><p>Already have an account? <Link href="/login">Sign in</Link></p></section></main>}
